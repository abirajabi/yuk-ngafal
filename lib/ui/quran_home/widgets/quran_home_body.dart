import 'package:flutter/material.dart';

class QuranHomeBody extends StatelessWidget {
  const QuranHomeBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CustomScrollView(
      slivers: [],
    );
  }
}
